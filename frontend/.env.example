VITE_API_URL=http://localhost:8080
VITE_API_KEY=your-api-key-here

# Free MapTiler key for dark-mode basemap (https://maptiler.com).
# Without it, dark mode falls back to Stadia tiles which now require auth.
VITE_MAPTILER_KEY=
